# Дневник растений 🌱

**Авторы:** Клочкова Самарин

Приложение для ухода за растениями с функциями ведения дневника, напоминаний и отслеживания прогресса через фотографии.

## Функциональность

- 📝 **Дневник растений** - ведение записей о каждом растении
- 🔔 **Напоминания ухода** - автоматические напоминания о поливе, удобрении и других действиях
- 📷 **Фото прогресса** - загрузка и просмотр фотографий для отслеживания роста растений
- 👤 **Аутентификация** - JWT-based авторизация пользователей

## Технологии

- **Backend:** FastAPI (Python)
- **Frontend:** HTML, CSS, JavaScript (веб-интерфейс)
- **Telegram Bot:** Python Telegram Bot (альтернативный интерфейс)
- **База данных:** PostgreSQL
- **Контейнеризация:** Docker & Docker Compose
- **Аутентификация:** JWT (JSON Web Tokens)

## Структура проекта

```
дневник роста/
├── backend/          # FastAPI приложение
│   ├── main.py      # Основной файл API
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/         # Веб-интерфейс
│   ├── index.html
│   ├── style.css
│   └── app.js
├── telegram_bot/     # Telegram бот
│   ├── bot.py       # Основной файл бота
│   ├── requirements.txt
│   └── Dockerfile
├── docker-compose.yml
├── nginx.conf
└── README.md
```

## Установка и запуск

### Требования

- Docker
- Docker Compose

### Запуск приложения

1. Клонируйте репозиторий или перейдите в папку проекта:
```bash
cd "дневник роста"
```

2. (Опционально) Для Telegram бота создайте бота через [@BotFather](https://t.me/BotFather) и получите токен. Затем установите переменную окружения:
```bash
export BOT_TOKEN=your_bot_token_here
```

3. Запустите все сервисы через Docker Compose:
```bash
docker compose up -d
```

4. Приложение будет доступно по адресам:
   - Frontend (веб): http://localhost
   - Backend API: http://localhost:8000
   - API документация: http://localhost:8000/docs
   - Telegram Bot: найдите вашего бота в Telegram и отправьте `/start`

### Остановка приложения

```bash
docker-compose down
```

Для полной очистки (включая данные БД):
```bash
docker-compose down -v
```

## Использование

### Веб-интерфейс

1. Откройте http://localhost в браузере
2. Зарегистрируйтесь или войдите в систему
3. Добавьте свое первое растение
4. Ведите дневник ухода, загружайте фото и настраивайте напоминания

### Telegram бот

1. Найдите вашего бота в Telegram (имя бота, которое вы указали при создании через @BotFather)
2. Отправьте команду `/start`
3. Зарегистрируйтесь или войдите в систему через интерактивные кнопки
4. Используйте меню для управления растениями, добавления записей, фото и напоминаний

**Команды бота:**
- `/start` - Главное меню
- Все действия выполняются через интерактивные кнопки

## API Endpoints

### Аутентификация
- `POST /register` - Регистрация нового пользователя
- `POST /token` - Получение JWT токена
- `GET /users/me` - Информация о текущем пользователе

### Растения
- `GET /plants` - Список всех растений пользователя
- `POST /plants` - Добавить новое растение
- `GET /plants/{id}` - Информация о растении
- `DELETE /plants/{id}` - Удалить растение

### Записи дневника
- `GET /plants/{id}/entries` - Все записи о растении
- `POST /plants/{id}/entries` - Добавить запись

### Фотографии
- `GET /plants/{id}/photos` - Все фото растения
- `POST /plants/{id}/photos` - Загрузить фото

### Напоминания
- `GET /plants/{id}/reminders` - Напоминания для растения
- `POST /plants/{id}/reminders` - Создать напоминание
- `GET /reminders/upcoming` - Предстоящие напоминания
- `POST /reminders/{id}/complete` - Отметить напоминание как выполненное

## База данных

Приложение использует PostgreSQL с следующими основными таблицами:
- `users` - пользователи
- `plants` - растения
- `plant_entries` - записи дневника
- `plant_photos` - фотографии
- `reminders` - напоминания

## Безопасность

⚠️ **Важно:** В production окружении обязательно измените `SECRET_KEY` в `docker-compose.yml` на надежный случайный ключ!

## Лицензия

Проект создан в учебных целях.

