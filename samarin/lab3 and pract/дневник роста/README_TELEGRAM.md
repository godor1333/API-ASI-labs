# Telegram Бот для Дневника растений 🤖

## Настройка

1. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
2. Получите токен бота
3. Создайте файл `.env` в корне проекта или установите переменную окружения:

```bash
export BOT_TOKEN=your_bot_token_here
```

Или создайте файл `.env`:
```
BOT_TOKEN=your_bot_token_here
```

4. Обновите `docker-compose.yml`, добавив переменную окружения, или используйте файл `.env`

## Запуск

```bash
docker compose up -d
```

Бот будет доступен в Telegram после запуска всех сервисов.

## Команды бота

- `/start` - Главное меню
- Регистрация и вход через интерактивные кнопки
- Управление растениями через меню
- Добавление записей, фото и напоминаний

## Функции

- ✅ Регистрация и авторизация
- ✅ Просмотр списка растений
- ✅ Добавление новых растений
- ✅ Ведение дневника (записи)
- ✅ Загрузка фото прогресса
- ✅ Настройка напоминаний
- ✅ Просмотр предстоящих напоминаний

