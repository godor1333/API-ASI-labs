# ⚠️ Остановка загрузки моделей

## Что произошло:

Проект пытался загрузить модель SDXL (~10+ ГБ) с HuggingFace. Это было не нужно для демонстрации.

## ✅ Что исправлено:

1. **По умолчанию используется stub-режим** - без загрузки больших моделей
2. **Добавлена защита** - перед загрузкой будет запрос подтверждения
3. **Настройка через конфиг** - можно легко переключать режимы

## 🚀 Теперь можно запустить снова:

```bash
python main.py full "кот полицейский на велосипеде"
```

**Или через start.bat:**
- Выберите режим 2
- Введите текст

## 📝 Что будет работать:

- ✅ Генерация stub-кадров (простые градиенты)
- ✅ Создание видео из кадров
- ✅ Склейка клипов
- ✅ Оценка через Webench2 (все метрики)
- ✅ Веб-интерфейс

## 💡 Для реальных изображений:

Если в будущем понадобятся реальные изображения, можно:
1. Изменить `config/config.yaml`: `method: "sdxl"`
2. Или установить переменную: `set DISABLE_SDXL_AUTO_LOAD=false`

Но для демонстрации пайплайна это не нужно!
