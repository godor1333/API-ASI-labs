# Работа без загрузки больших моделей

## ✅ Режим stub-кадров (по умолчанию)

Проект настроен на работу **без загрузки больших моделей** (10+ ГБ). 

### Что это значит:

- ✅ Проект работает и демонстрирует весь пайплайн
- ✅ Генерируются stub-кадры (заглушки) вместо реальных изображений
- ✅ Видео создаётся и склеивается корректно
- ✅ Оценка через Webench2 работает полностью
- ❌ Изображения будут простыми градиентами, а не реальными картинками

### Текущие настройки:

В `config/config.yaml` установлено:
```yaml
generation:
  keyframe_generation:
    method: "stub"  # Используются stub-кадры
```

## 🔄 Как включить реальную генерацию (если нужно):

### Вариант 1: Через конфиг
Измените в `config/config.yaml`:
```yaml
generation:
  keyframe_generation:
    method: "sdxl"  # Включить SDXL
```

### Вариант 2: Через переменную окружения
```bash
set DISABLE_SDXL_AUTO_LOAD=false
python main.py full "ваш текст"
```

## 📝 Что работает в stub-режиме:

1. ✅ Парсинг текста на сцены
2. ✅ Генерация ключевых кадров (stub)
3. ✅ Генерация клипов (через ComfyUI или stub)
4. ✅ Склейка видео
5. ✅ Оценка через Webench2:
   - CLIPScore (если CLIP модель загружена)
   - ImageReward (упрощённая версия)
   - Технические проверки (статичные кадры, яркость и т.д.)

## 💡 Рекомендация:

Для демонстрации и тестирования пайплайна **stub-режим идеален** - всё работает быстро без загрузки 10+ ГБ.

Для реальной генерации красивых изображений нужны модели, но это опционально.
