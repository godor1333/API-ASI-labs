# 🌀 LabyrinthAI: Vision-Based Maze Runner

Проект по реализации интеллектуального агента, который проходит лабиринты, используя только компьютерное зрение. Никаких читов с координатами — агент «видит» мир через нейронки и сам находит путь.

## 🚀 Основные фишки

* **Dual-Model Vision**: Связка из **ViT (Vision Transformer)** для классификации и **DETR (Detection Transformer)** для точного поиска объектов.
* **Memory Optimization**: Реализован паттерн **Model Registry (Singleton)** — веса моделей грузятся в память всего один раз, экономя оперативу.
* **Performance Tracking**: Встроенные бенчмарки через `torch.utils.benchmark` для контроля скорости инференса.
* **Post-Run Analytics**: Автоматическая генерация **Heatmaps** (тепловых карт) и инфографики с уверенностью модели после каждого захода.

## 📊 Результаты тестов

Система полностью покрыта автотестами (`pytest`). Результаты последних прогонов:

| Метрика | Значение | Статус |
| :--- | :--- | :--- |
| **8/8 Tests Passed** | Стабильность, парсинг, инференс | ✅ PASSED |
| **ViT Median Time** | ~571 ms | ⚡ Fast |
| **DETR Median Time** | ~2.50 s | 🎯 Precise |
| **Confidence Score** | 98% | 🔥 Reliable |

## 🛠 Как запустить

1.  **Установка зависимостей**:
    ```bash
    pip install -r requirements.txt
    ```

2.  **Запуск тестов и бенчмарков**:
    ```bash
    python -m pytest tests -v -s
    ```
    *Это подтвердит, что всё работает чётко и веса моделей на месте.*

3.  **Запуск агента в лабиринте**:
    ```bash
    python main.py
    ```

## 📈 Визуализация
После работы загляни в папку `logs/`:
* `inference_stats.png` — покажет, насколько нейронка была уверена в своих ответах.
* `heatmap.png` — визуализация маршрута и зон внимания ИИ.

---
**Разработано для защиты проекта LabyrinthAI. Всё круто, клево, прикольно.** ✌️